server:
  applicationConnectors:
    - type: http
      port: 9000
  adminConnectors:
    - type: http
      port: 9001

## Database settings.
database:
  # the name of the JDBC driver, mysql in our case
  driverClass: com.mysql.jdbc.Driver
  # the username
  user: dropwizardsample
  # the password
  password: Cq0!~jmc9pG9
  # the JDBC URL;
  url: jdbc:mysql://den1.mysql1.gear.host:3306/dropwizardsample

# Database settings.
#database:
#  # the name of the JDBC driver, mysql in our case
#  driverClass: com.mysql.jdbc.Driver
#  # the username
#  user: ${DW_DB_USER}
#  # the password
#  password: ${DW_DB_PASSWORD}
#  # the JDBC URL;
#  url: ${DW_DB_URL}

